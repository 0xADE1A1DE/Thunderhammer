Here we include some example powershell scripts for priming aggressor and victim rows, hammering, and reading out a particular victim offset

To run on windows: 
```sh
powershell -ExecutionPolicy Bypass -File .\<filename>.ps1
```
